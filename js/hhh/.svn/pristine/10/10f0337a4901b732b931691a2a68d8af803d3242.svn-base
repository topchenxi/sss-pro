import * as Types from 'src/vuex/constants/index'
const state = {
  tabIndex: '1'
}

// mutations
const mutations = {
  [Types.UPDATE_PARESSPAYDEBT_CURRENT_TAB] (state, params) {
    state.tabIndex = params.tabIndex
  }
}

export default {
  state,
  mutations
}
